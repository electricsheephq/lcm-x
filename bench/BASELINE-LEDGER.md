# BASELINE LEDGER — merged changes that move eval baselines

Rule (architect, 2026-08-20, #252 verdict batch): a scoreboard row may be compared against a
row from the other side of a listed boundary ONLY after the noted re-baseline has run. Rows
from pinned non-main trees (e.g. the LoCoMo product worktree) are unaffected by main-side
boundaries. Boundaries are appended, never edited.

| boundary (merge) | date | surface | required re-baseline | status |
|---|---|---|---|---|
| #192 assembly adjacent-assistant row-merge | 2026-08-19 | answer-quality rows (assembled context) | any future main-tree LoCoMo/AMA answer row records "post-#192" | OPEN — no main-tree answer row exists yet |
| #199 preflight maintenance deferral | 2026-08-19 | compaction timing on default path | next full-500 V1-M run doubles as re-baseline (deterministic); LoCoMo on next main-tree config. Containment note: both post-#199 benchmark trees reproduced the pre-#199 95q subset BITWISE → empirically inert on the V1-M declared config | PARTIALLY DISCHARGED (subset evidence); full-500 pending |
| #245 summary-contract acceptance widened | 2026-08-19 | summary ingest (fail-open widening) | ledger entry (this row) + zero-moved-rows confirmation on next full run | OPEN — check on next full run |
| #180 query arg coercion | 2026-08-19 | AMA/agentic arms only | scoped re-baseline on next AMA row. Retrieval rows PROVABLY inert: only retro PR inside the F56 comparison trees; F56 non-override rows bitwise-identical | OPEN (AMA); DISCHARGED (retrieval) |
| #263-fix (gpt-5.6 OAuth cap 372K→272K) | 2026-08-21 | compaction cadence on gpt-5.6 codex-OAuth routes where the host advertises >272K (threshold now computed on the real window; compaction earlier, overflow risk closed) | banked rows predate and pinned their effective windows (F53 tree-pinned; F59 arms recorded windows per-arm); next main-tree row on a 5.6 OAuth route records post-#263-fix | OPEN |
| #332/#333/#338 cloud-embedding privacy transform (privacy trio, v0.23.1) | 2026-08-23 | **production recall-path content shaping (proven boundary, audited 2026-08-26)**: `protect_embedding_text` shapes what the PRODUCTION recall path embeds when sensitive patterns are enabled; patterns default OFF (config.py:701) and a cloud-provider config then raises — pre-#370 `lcm_recall` could swallow that raise into a silent FTS degrade (#367). Durable sensitive redaction PRE-DATES the trio (6376272c, ancestor of v0.22.0) and the trio's transform does not mutate durable source. HARNESS GAP CLOSED (#370, refined by #374): `benchmarking/longmemeval.py` now applies the production provider-copy transform via `_embedding_privacy_context()` on both the summary and chunk paths, and constructs its per-question and template configs with `sensitive_patterns_enabled=False, embedding_privacy_enabled=None` — the lossless-durable / protected-provider-copy posture production ships. Benchmark and production embedding paths no longer diverge by construction; the re-bank run still has to settle F53 reproducibility empirically | **cross-boundary comparisons held pending verification**: F53 reproducibility on current main is UNVERIFIED (source-level analysis only, #367 — no completed rerun). The re-bank run must (a) record the harness privacy posture (now: durable lossless + provider-copy transform on, matching production), and (b) settle reproducibility empirically. Ledger row was MISSING at merge (reconciled 2026-08-26; boundary corrected same day per the independent audit) | OPEN |
| #352 V1-M provider identity + accounting | 2026-08-24 | benchmarking/longmemeval.py instrument (provider identity/resume/accounting) | cross-#352 comparisons FROZEN until discharged by a concrete event: the next full-500 V1-M run on the post-#352 instrument (the F53 re-bank registration, sequenced post-v0.23.2) completes and its finding records "post-#352 instrument" — that run discharges this row to RECORDED. (Original verdict of record: LAND-AFTER-REBASELINE, #252 2026-08-24 11:09; ledger entry was comment-only until this row — reconciled 2026-08-26) | OPEN |

Standing caveats adopted from the #252 retro pack (recorded, no re-baseline owed):
- #158 / #170: if any bench profile ever routes through `LCMConfig.from_env()`, per-model
  threshold overrides and `LCM_ABSOLUTE_THRESHOLD_TOKENS` silently outrank the ratio-based
  setpoint and would not appear in the run's own config record. Same trap class as
  RUN-SHEET-V1M-RERANK-ON §1 (explicit-ctor vs from_env).
- #212: `LCM_DISABLED_TOOLS` on a customer box is a memory-coverage change (disabled-tool
  turns stop entering the corpus), not just a prompt-size change.
- #211: a SiliconFlow embedding arm, if ever wanted, enters the scoreboard as a NEW row,
  never as a delta against F53.

Rows appended by the v0.23.0 maintainer train (authorized by the architect ruling, #252
comment 5356005454; classes per its derivation rule; ARCHITECT-TBD cells to be filled by the
architect):

| boundary (merge) | date | surface | required re-baseline | status |
|---|---|---|---|---|
| #168 durable compaction totals in status | 2026-08-20 | status/observability output only | ledger entry only, no re-baseline owed | RECORDED |
| #196 expand-query evidence provenance | 2026-08-20 | answer/assembly (expand-query provenance object) | next main-tree answer row records "post-#196" | OPEN — no main-tree answer row exists yet |
| #173 recall FTS sub-budget + hardened corpus preflight | 2026-08-20 | retrieval path (FTS/vector budget split; scan-budget, session-scope, expiry-evidence preflight; timeout-semantics fix ed586a27) | next full-500 V1-M run + next main-tree LoCoMo row | OPEN |
| #203 replay scaffolding normalization + OOB unique-identity binding | 2026-08-20 | ingest/reconcile (replay proofs; ID-less rows fail closed) | corpus-shaping: next full-500 V1-M run re-baselines (deterministic) WITH an ingest corpus-count parity check vs F53 (fail-closed ID-less drops must show up as count deltas, never silently); next main-tree LoCoMo row records post-#203 | OPEN |
| #273 crashed-search disclosure (coverage 'none', never 'ok') | 2026-08-20 | disclosure-only, crash paths | ledger entry only, no re-baseline owed (verdict 5355967878) | RECORDED |
| #177 partial tool tail replay: occurrence-bounded proofs + tool-name identity | 2026-08-20 | ingest/reconcile (replay identity 5-tuple; replay-snapshot digests change once post-upgrade — persists instead of matching) | corpus-shaping, replay-heavy corpora only: bench corpora ingest fresh (no replay tails) so declared configs are expected-inert — verified by the same V1-M corpus-count parity check as #203; any future bench that REPLAYS real sessions (e.g. the compaction program P1) must re-baseline against post-#177 digests explicitly | OPEN |
| #261 startup FTS bootstrap race + integrity-state publish contract | 2026-08-20 | startup bootstrap (verified-pass clearing; CAS-fenced scan publish) | FTS-availability at process start: next main-tree LoCoMo row (FTS-sensitive; C1-lineage runs start fresh processes, so a fixed bootstrap race can only move FTS-arm coverage upward — treat as part of the joint FTS program decision). V1-M declared config unaffected while its reference fts arm stays dark (F49 class) | OPEN |
| #183 cross-session summary DAG expansion | 2026-08-21 | retrieval path (expand/describe/expand_query cross-session; provenance session attribution) | next full-500 V1-M run + next main-tree LoCoMo row | OPEN |
| #286 teams-slice-1 AccessContextV1 contract | 2026-08-21 | additive, eval-unreachable (all-new paths; verdict 5356722388) | no re-baseline owed | RECORDED |
| #297 auto-focus keeps newest request (summary-steering) | 2026-08-21 | compaction/assembly focus string for over-cap user turns (ingest-side summarization included) | next full-500 V1-M run re-baselines (deterministic); next main-tree LoCoMo row records post-#297 | OPEN |
| #300 teams-slice-2 policy/catalog/scope-storage | 2026-08-21 | additive, eval-unreachable (all-new paths, zero call sites; verdict 5358203472) | no re-baseline owed | RECORDED |

Rows appended by the v0.23.2 security/privacy train (architect, 2026-09-05; merge shas derived with
`gh pr view <n> --json mergeCommit`; the single discharge event for every OPEN row below is the F53
re-bank registration, #367/#380, which runs on the shipped posture — durable lossless + provider-copy
transform ON — and registers as a NEW row, never as a delta against F53's raw-embed config). **Executed
2026-09-05 and PARKED at the pre-spend gate (FINDING-F62)**: the shipped posture re-shapes 599 unit occurrences and refuses 18 more (§4.2 transform-change count — returned
`changed=True` OR raised — 617 occurrences = 93 unique units in 258 of 500 questions), so the boundary below is measured LIVE on LongMemEval-M and no successor row exists; statuses updated
in place, rows unchanged, next step is an owner decision on #380:

| boundary (merge) | date | surface | required re-baseline | status |
|---|---|---|---|---|
| #364 read-only current-schema clones (9e43907f; closes #363) | 2026-08-26 | compaction path under concurrent SQLite writers: a Hermes per-agent host clone whose current-schema startup hit a busy WAL fell back silently to the lossy compressor; startup is now read-only at the two idempotent migration/repair boundaries | bench-inert by construction: `benchmarking/longmemeval.py` builds `MessageStore` / `SummaryDAG` / `VectorStore` directly and never runs `LCMEngine` compaction or clone startup, so no bench row can observe this boundary and none is owed a re-baseline; the observable gate is the multi-clone live soak (release-gauntlet Phase C), which already ran post-#364 on the rc3/GA tree (59bba769 ⊇ 9e43907f) with the pre-existing #247 conflicts as its only finding | RECORDED — bench-inert by construction; live-soak gate discharged at rc3/GA |
| #366 private-key redaction ordering (bc033109; SECURITY #365) | 2026-08-26 | **provider-copy content shaping** (`protect_embedding_text`): private-key patterns apply before assignment patterns (e.g. `password_assignment`) on both durable and provider paths, and a transform-independent residual validator fails closed (raise) rather than dispatching a leaked key body; transform identity privacy:v1→v3 (`ingest_protection.py:396` on main), so every cloud vector identity re-embeds. Durable path moves only when `sensitive_patterns_enabled=True` (default False, `config.py:702`) | F53 re-bank registers privacy:v3 + the provider-copy posture; a corpus without credential-shaped material is expected-inert, but the re-bank must report the TRANSFORM-CHANGE COUNT — documents for which `protect_embedding_text` returned `changed=True` or raised, measured by a full prewarm pass over the prepared corpus (input-hash parity; the harness has no blocked counter and a redaction changes text without blocking) — 0 means inert for that corpus, >0 means the boundary is live and the row moves | OPEN — **MEASURED LIVE 2026-09-05 (F62 §3/§8a)**: §4.2 transform-change count 617 occurrences = 93 unique units across 258 of 500 questions — 599 occurrences / 90 units / 248 questions re-shaped (placeholders password_assignment / api_key / private_key; query texts 0 changed) plus 3 distinct units REFUSED by the residual validator (18 occurrences, 17 questions; 7 questions carry both), so the registered run could not proceed past the gate; not inert; F53 stays the row of record; successor row pending the owner decision on #380 |
| #370 privacy policy errors fail loud on the recall path (f21fc84b; closes #367 ask 3 — ask 1, the trio ledger row, landed via #368 b2142ed8) | 2026-08-26 | INSTRUMENT + recall path: `lcm_recall` no longer swallows `EmbeddingPrivacyPolicyError` into a silent FTS degrade (four handler sites; proactive-recall counter + warn-once); `benchmarking/longmemeval.py` now applies the production provider-copy transform on the run path (`evaluate_question` builds an `embedding_privacy_enabled=None` config and calls `protect_embedding_text` directly, ~:2405-2743) and via `_embedding_privacy_context` (:478) in the prewarm/determinism paths (:1588/:1655) | the loud-fail half moves no baseline by construction (a run that would have silently degraded now stops loud; already reflected in the privacy-trio row's HARNESS GAP CLOSED clause); the harness-transform half is NOT inert by construction — `protect_embedding_text` can rewrite a document or query (`changed=True`) without raising, so it is the same provider-copy boundary as #366/#374 and is discharged by the F53 re-bank's transform-change count (see the #366 row) | OPEN — loud-fail half RECORDED; harness-transform half **MEASURED LIVE 2026-09-05 (F62 §3/§8a)**: the same provider-copy boundary as #366 (that row's §4.2 count 617 > 0), not inert; the re-bank PARKED with no successor row; pending the owner decision on #380 |
| #374 lossless by default (fbe24cce; closes #371 #346-amendment) | 2026-08-26 | **recall-path content shaping in both directions**: (a) the durable store is lossless unless `sensitive_patterns_enabled=True` — v0.23.1 configs that forced durable redaction under a cloud provider now retain raw text, so FTS hits and rerank snippets see raw; (b) provider copies — embed documents, embed query (`tools.py:3762`), rerank query + snippets (`tools.py:5457/5461`) — pass through the transform by default for cloud providers via `embedding_privacy_enabled` (`config.py:707`, None = auto-on for cloud), and the explicit opt-out is a distinct vector identity (`privacy:off`) | F53 re-bank on the shipped posture; the F53 declared config predates the flag and embedded raw text with no transform, so the re-bank is a new row registration | OPEN — the registered re-bank PARKED 2026-09-05 (F62): the posture this row ships cannot embed the LongMemEval-M corpus (3 refused units) and re-shapes 599 unit occurrences; no successor row; pending the owner decision on #380 |
| #384 + #391 truncated-PEM scanner (c0402f31 + 59bba769; rc1/rc2 gauntlet catches, #383 #389) | 2026-08-26 / 2026-08-27 | provider-copy content shaping: marker-independent backstops (orphan full-width base64 runs; raw-text proximity-to-placeholder backstop restored with git-SHA hex exclusion) make a chunk carrying PEM-shaped material near a redacted key fail closed (embed refused) instead of dispatching; #391 removes the key-plus-git-SHA over-block #384 introduced | same discharge event as #366/#374; expected-inert on PEM-free corpora, verified by the re-bank's transform-change count == 0 (see the #366 row) | OPEN — **expected-inert REFUTED 2026-09-05 (F62 §8a)**: the §4.2 transform-change count is 617 (599 re-shaped + 18 refused; #366 row) and the #384 orphan-full-width-base64 backstop alone refused 3 corpus units that carry no PEM marker and no "private key" text (`PREFIXED_B64` lines measured on prefix+tail; one is plain prose) — a measured precision boundary of the shipped scanner on marker-free text — the three refused units carry no PEM marker (F62 §8a), while the corpus itself does contain PEM material: two of the 90 re-shaped units carry a private key that the transform redacted (`private_key` class in `changed-units-classes.json`; markers confirmed by replay, `pem-marker-check-private-key-units.json`) — so this is precision on marker-free text, not a corpus without keys (flagged on #394; redaction changes are owner-gated) |
| #155 session-end prefix matching extracted to a mixin (a84ca050) | 2026-09-04 | compaction path refactor only; maintainer AST-identity check: 18 moved methods, 18 identical, 0 different, no shadowing (acceptance-155 receipt) | ledger entry only, no re-baseline owed | RECORDED |
