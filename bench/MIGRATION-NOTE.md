# Benchmark program docs — migration note

Migrated **2026-08-19** from `100yenadmin/hermes-lcm@docs/program-architecture`
(frozen at `64d6d3d49744cf47604e1670d8221167d96555f1`), plus the R2-era program docs
extracted from lcm-x PR #200's branch (`origin/upstream/pr-504` @ `eb7b22a`).

Every filename PR #200 carried was already present in the frozen docs branch at equal
or newer content, so the R2-era extraction contributed no net-new files; the docs-branch
`bench/PROGRAM-ARCHITECTURE.md` is a strict superset of PR #200's (524 vs 345 lines,
zero lines unique to PR #200) and wins the name.

**The benchmark program's history of record now lives here**, in this repo's `bench/`.
