Release candidate 1 for v0.23.0 — the correctness batch (data-integrity/replay-proof fixes,
retrieval hardening, cross-session expansion, ops fixes, and the dormant Teams slice-1
contract). Full details in the v0.23.0 notes on this branch. RC gates: full
`validate_release.sh --full` at this tag (CI green at the tagged commit is the arbiter for
the two documented local-env failure classes), plus the eva-profile upgrade rehearsal
(v0.20.0 fleet-cut donor) and a live-session soak before the final tag.
